[project]
name = "heat-transfer-recipe"
version = "0.1.0"
description = "Heat transfer surrogate modeling recipe for the Noether framework"
requires-python = ">=3.12"
dependencies = [
    "emmiai-noether",
    "plotly",
    "wandb>=0.25.1",
]

# Remove the following if you are outside the Noether repo
[tool.uv.sources]
emmiai-noether = { path = "../../", editable = true }
